import React from 'react';
import { PageTitleEng } from '../../../styles/CommonStyled';

const LoginTitle = (): JSX.Element => {
	return <PageTitleEng>WISHARE LOGIN</PageTitleEng>;
};

export default LoginTitle;
