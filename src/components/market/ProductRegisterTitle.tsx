import React from 'react';
import { PageTitle } from '../../styles/CommonStyled';

const ProductRegisterTitle = (): JSX.Element => {
	return <PageTitle>상품 등록</PageTitle>;
};

export default ProductRegisterTitle;
